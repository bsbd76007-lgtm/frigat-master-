node_modules/
dist/

# Secrets. .env.example is the committed template and must survive the
# .env.* wildcard above it — without the negation, the template this repo
# tells contributors to copy would itself be untracked.
.env
.env.*
!.env.example
!apps/*/.env.example


.next/
*.tsbuildinfo

coverage/
.turbo/
.cache/

*.log
.DS_Store
.claude
.vscode/
CLAUDE.md
graphify-out
.claudeignore
.env.production
